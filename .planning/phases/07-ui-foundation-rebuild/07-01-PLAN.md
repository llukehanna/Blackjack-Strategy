---
phase: 07-ui-foundation-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BJS/Design/BJSColors.swift
  - BJS/Design/Typography.swift
  - BJS/Design/Spacing.swift
  - BJS/Design/CornerRadius.swift
  - BJS/Design/Elevation.swift
  - BJS/Design/AnimationTiming.swift
  - BJS/Views/Trainer/SessionStartView.swift
  - BJS/Views/Trainer/SessionSummaryView.swift
  - BJS/Views/Trainer/TrainerView.swift
  - BJS/Views/Trainer/CardView.swift
  - BJS/Views/Trainer/HandView.swift
  - BJS/Views/Trainer/ActionButtonsView.swift
  - BJS/Views/Trainer/FeedbackOverlayView.swift
  - BJS/Views/Common/StatsBarView.swift
  - BJS/Views/Common/SectionContainerView.swift
  - BJS/Views/Common/TrainingModeToggle.swift
  - BJS/App/BJSApp.swift
  - BJSTests/DesignTokenTests.swift
autonomous: true
requirements: [UI-07-D1, UI-07-D2, UI-07-D9, UI-07-D11, UI-07-D14, UI-07-D15]

must_haves:
  truths:
    - "All 16 color tokens, 3 typography roles, 6 spacing values, 5 corner-radius tokens, 2 elevation tokens, 4 animation-timing tokens exist with the exact values from 07-UI-SPEC Token Files block"
    - "Project compiles green with zero references to deleted token symbols"
    - "DesignTokenTests asserts every UI-SPEC token symbol holds the specified value"
  artifacts:
    - path: BJS/Design/BJSColors.swift
      provides: "16 color tokens (caseless enum)"
    - path: BJS/Design/Typography.swift
      provides: "caption/body/title only"
    - path: BJS/Design/Spacing.swift
      provides: "xs/sm/md/lg/xl/xxl"
    - path: BJS/Design/CornerRadius.swift
      provides: "card/button/overlayButton/overlay/navCircle"
    - path: BJS/Design/Elevation.swift
      provides: "card/overlay"
    - path: BJS/Design/AnimationTiming.swift
      provides: "tap/overlayIn/overlayOut/cardDeal"
    - path: BJSTests/DesignTokenTests.swift
      provides: "token value assertions"
  key_links:
    - from: "every existing view file"
      to: "BJS/Design/*.swift"
      via: "token symbol references"
      pattern: "BJSColors\\.|Typography\\.|Spacing\\.|CornerRadius\\.|Elevation\\.|AnimationTiming\\."
---

<objective>
Rewrite the entire BJS/Design/ token set to the strict minimal palette in 07-UI-SPEC, and mechanically migrate every existing view file (in-scope and out-of-scope) to the new symbols so the build stays green. Add Wave-0 DesignTokenTests asserting every token holds the spec value.

Purpose: Lock the token system that every other Phase 7 plan composes against. Pitfall 6 in 07-RESEARCH.md is the dominant execution risk — token rewrite without same-commit migration breaks compilation across SessionStartView, SessionSummaryView, StatsBarView, etc. This plan addresses it atomically.

Output: Six rewritten token files, eleven view files migrated, one new test file. No view file references a deleted token after this plan.
</objective>

<execution_context>
@/Users/luke/BJS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/BJS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-foundation-rebuild/07-CONTEXT.md
@.planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
@.planning/phases/07-ui-foundation-rebuild/07-RESEARCH.md
@CLAUDE.md

Existing token convention (Phase 02.1): caseless enum namespaces with static let.
Existing init pattern: Color(red: 0xNN/255, green: 0xNN/255, blue: 0xNN/255). Do NOT introduce Color(hex:).

Symbols DELETED this plan (must be replaced wherever referenced):
  Typography.heroStat, Typography.playerTotal, Typography.statValue, Typography.mono
  BJSColors.cardFaceDown, BJSColors.feedbackCorrectIcon, BJSColors.feedbackCorrectBorder,
  BJSColors.feedbackIncorrectIcon, BJSColors.feedbackIncorrectBorder
  Elevation.cardShadowColor
  Any adaptive UIColor dynamic-provider color (UI-07-D1 reverses adaptive pattern)

New symbols created (verbatim from 07-UI-SPEC "Token Files — Required Symbols"):
  BJSColors: surfaceBase, surfaceRaised, actionDark, surfaceOverlay, borderSubtle,
             borderOnOverlay, accentGold, actionLabel, watermarkInk, textPrimary,
             textSecondary, textOnOverlay, textOnOverlayMuted, feedbackCorrect,
             feedbackIncorrect, cardBackRed
  Typography: caption (12pt bold, +1.5 tracking applied at use site),
              body (16pt regular), title (22pt bold)
  Spacing: xs=4, sm=8, md=16, lg=24, xl=48, xxl=64
  CornerRadius: card=8, button=0, overlayButton=12, overlay=24, navCircle=16
  Elevation: card (black @ 0.4, radius 12, y 6), overlay (black @ 0.6, radius 24, y -8)
  AnimationTiming: tap (120ms easeOut), overlayIn (280ms easeOut),
                   overlayOut (200ms easeIn), cardDeal (320ms easeOut, 60ms stagger)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite all six token files to 07-UI-SPEC values</name>
  <read_first>
    - .planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
    - BJS/Design/BJSColors.swift
    - BJS/Design/Typography.swift
    - BJS/Design/Spacing.swift
    - BJS/Design/CornerRadius.swift
    - BJS/Design/Elevation.swift
    - BJS/Design/AnimationTiming.swift
  </read_first>
  <behavior>
    - Spacing.xs==4, sm==8, md==16, lg==24, xl==48, xxl==64
    - CornerRadius.card==8, button==0, overlayButton==12, overlay==24, navCircle==16
    - BJSColors holds the 16 named tokens with the RGB values from UI-SPEC Token Files block
    - Typography exposes exactly three roles: caption, body, title
    - Build: zero references to deleted symbols
  </behavior>
  <action>
Replace each of the six files in BJS/Design/ wholesale — no shims, no @available wrappers, no deprecated aliases. Use caseless `enum` namespace pattern (matches Phase 02.1). Use `Color(red: 0xNN/255, green: 0xNN/255, blue: 0xNN/255)` form — NOT Color(hex:).

BJSColors.swift — exact values per UI-SPEC:
  surfaceBase        = Color(red: 0x0A/255, green: 0x0A/255, blue: 0x0E/255)
  surfaceRaised      = Color(red: 0x16/255, green: 0x16/255, blue: 0x1B/255)
  actionDark         = Color(red: 0x1F/255, green: 0x1F/255, blue: 0x25/255)
  surfaceOverlay     = Color.white
  borderSubtle       = Color.white.opacity(0.08)
  borderOnOverlay    = Color(red: 0xD8/255, green: 0xD8/255, blue: 0xDC/255)
  accentGold         = Color(red: 0xE0/255, green: 0xA4/255, blue: 0x36/255)
  actionLabel        = Color(red: 0x6B/255, green: 0x84/255, blue: 0x99/255)
  watermarkInk       = Color(red: 0x3A/255, green: 0x58/255, blue: 0x68/255)
  textPrimary        = Color.white
  textSecondary      = Color.white.opacity(0.6)
  textOnOverlay      = Color(red: 0x0A/255, green: 0x0A/255, blue: 0x0E/255)
  textOnOverlayMuted = Color(red: 0x0A/255, green: 0x0A/255, blue: 0x0E/255).opacity(0.65)
  feedbackCorrect    = Color(red: 0x2B/255, green: 0xB6/255, blue: 0x73/255)
  feedbackIncorrect  = Color(red: 0xE5/255, green: 0x48/255, blue: 0x4D/255)
  cardBackRed        = Color(red: 0xB8/255, green: 0x28/255, blue: 0x28/255)

Typography.swift — three roles ONLY. Delete heroStat, playerTotal, statValue, mono.
  static let caption: Font = .system(size: 12, weight: .bold)
  static let body:    Font = .system(size: 16, weight: .regular)
  static let title:   Font = .system(size: 22, weight: .bold)
File-header comment: "caption expects .tracking(1.5) applied at use site per UI-07-D spec".

Spacing.swift — static let xs/sm/md/lg/xl/xxl: CGFloat = 4/8/16/24/48/64

CornerRadius.swift — static let card/button/overlayButton/overlay/navCircle: CGFloat = 8/0/12/24/16

Elevation.swift — struct Shadow { let color: Color; let radius: CGFloat; let x: CGFloat; let y: CGFloat }
  static let card    = Shadow(color: .black.opacity(0.4), radius: 12, x: 0, y: 6)
  static let overlay = Shadow(color: .black.opacity(0.6), radius: 24, x: 0, y: -8)

AnimationTiming.swift — four Animation constants plus stagger:
  static let tap        = Animation.easeOut(duration: 0.120)
  static let overlayIn  = Animation.easeOut(duration: 0.280)
  static let overlayOut = Animation.easeIn(duration: 0.200)
  static let cardDeal   = Animation.easeOut(duration: 0.320)
  static let cardDealStagger: Double = 0.060

DELETE every other token symbol from these files. No @available shims. No comments referencing old symbols.
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && grep -REc "heroStat|playerTotal|statValue|Typography\.mono|cardFaceDown|cardShadowColor" BJS/Design/ | grep -v ":0$" | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "static let surfaceBase" BJS/Design/BJSColors.swift returns 1
    - grep -c "static let cardBackRed" BJS/Design/BJSColors.swift returns 1
    - grep "0x0A/255.*0x0A/255.*0x0E/255" BJS/Design/BJSColors.swift matches (surfaceBase)
    - grep "0xE0/255.*0xA4/255.*0x36/255" BJS/Design/BJSColors.swift matches (accentGold)
    - grep "0xB8/255.*0x28/255.*0x28/255" BJS/Design/BJSColors.swift matches (cardBackRed)
    - grep -c "static let xxl" BJS/Design/Spacing.swift returns 1
    - grep "xxl.*=.*64" BJS/Design/Spacing.swift matches
    - grep -c "static let overlayButton" BJS/Design/CornerRadius.swift returns 1
    - grep "overlayButton.*=.*12" BJS/Design/CornerRadius.swift matches
    - grep -c "static let navCircle" BJS/Design/CornerRadius.swift returns 1
    - grep -RE "heroStat|playerTotal|statValue|cardFaceDown" BJS/Design/ returns no matches
    - BJS/Design/Typography.swift contains exactly three "static let" declarations
  </acceptance_criteria>
  <done>All six token files contain only the symbols listed in UI-SPEC Token Files block, with exact values, caseless enum namespaces, Color(red:green:blue:) form.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanically migrate every existing view file to new tokens</name>
  <read_first>
    - BJS/Views/Trainer/SessionStartView.swift
    - BJS/Views/Trainer/SessionSummaryView.swift
    - BJS/Views/Trainer/TrainerView.swift
    - BJS/Views/Trainer/CardView.swift
    - BJS/Views/Trainer/HandView.swift
    - BJS/Views/Trainer/ActionButtonsView.swift
    - BJS/Views/Trainer/FeedbackOverlayView.swift
    - BJS/Views/Common/StatsBarView.swift
    - BJS/Views/Common/SectionContainerView.swift
    - BJS/Views/Common/TrainingModeToggle.swift
    - BJS/App/BJSApp.swift
  </read_first>
  <action>
For every view file listed, perform mechanical symbol replacement so the project compiles. This is a build-green pass — visual fidelity of out-of-scope screens is NOT a goal. Where no direct equivalent exists, leave a `#warning("Phase 7: {file} uses placeholder token — will be re-skinned in a later phase")` and use the closest available token.

Replacement table (apply via Edit on each file):
  Typography.heroStat       -> Typography.title   (+ #warning)
  Typography.playerTotal    -> Typography.title   (+ #warning)
  Typography.statValue      -> Typography.body    (+ #warning)
  Typography.mono           -> Typography.body    (+ #warning)
  BJSColors.cardFaceDown    -> BJSColors.cardBackRed
  BJSColors.feedbackCorrectIcon   -> BJSColors.feedbackCorrect
  BJSColors.feedbackCorrectBorder -> BJSColors.feedbackCorrect
  BJSColors.feedbackIncorrectIcon   -> BJSColors.feedbackIncorrect
  BJSColors.feedbackIncorrectBorder -> BJSColors.feedbackIncorrect
  Elevation.cardShadowColor -> Elevation.card.color
  .shadow(color: Elevation.cardShadowColor, radius: X, x: 0, y: Y) -> .shadow(color: Elevation.card.color, radius: Elevation.card.radius, x: Elevation.card.x, y: Elevation.card.y)
  Any deleted Spacing/CornerRadius/AnimationTiming symbol -> nearest neighbor + #warning

Do NOT redesign any view in this task — replacements only. TrainerView, CardView, HandView, FeedbackOverlayView, ActionButtonsView will be deleted+rewritten in Plans 02 and 03; here they only need to compile.

After replacement, run XcodeGen if project.yml changed (it should not — no file additions), then build. Fix any residual compile errors by choosing a plausible nearest token + #warning.
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && xcodebuild build -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -20 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - grep -RE "heroStat|playerTotal|statValue|Typography\.mono|cardFaceDown|feedbackCorrectIcon|feedbackCorrectBorder|feedbackIncorrectIcon|feedbackIncorrectBorder|cardShadowColor" BJS/Views BJS/App returns no non-comment matches
    - xcodebuild build emits BUILD SUCCEEDED
    - grep -rc "#warning" BJS/Views/ may be non-zero (acceptable for out-of-scope screens)
  </acceptance_criteria>
  <done>Project compiles green. Every out-of-scope view references only symbols defined in the rewritten token files. #warning markers are acceptable on out-of-scope screens.</done>
</task>

<task type="auto">
  <name>Task 3: Create DesignTokenTests.swift asserting all token values</name>
  <read_first>
    - .planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
    - BJS/Design/BJSColors.swift (after Task 1)
    - BJSTests/CasinoPresetTests.swift (for Swift Testing file template)
  </read_first>
  <action>
Create BJSTests/DesignTokenTests.swift using Swift Testing (`import Testing`, `@Test`). Assert every token from UI-SPEC Token Files block:

  @Test func spacingValues() {
    #expect(Spacing.xs == 4)
    #expect(Spacing.sm == 8)
    #expect(Spacing.md == 16)
    #expect(Spacing.lg == 24)
    #expect(Spacing.xl == 48)
    #expect(Spacing.xxl == 64)
  }

  @Test func cornerRadiusValues() {
    #expect(CornerRadius.card == 8)
    #expect(CornerRadius.button == 0)
    #expect(CornerRadius.overlayButton == 12)
    #expect(CornerRadius.overlay == 24)
    #expect(CornerRadius.navCircle == 16)
  }

  @Test func elevationValues() {
    #expect(Elevation.card.radius == 12)
    #expect(Elevation.card.y == 6)
    #expect(Elevation.overlay.radius == 24)
    #expect(Elevation.overlay.y == -8)
  }

  @Test func animationTimingExists() {
    _ = AnimationTiming.tap
    _ = AnimationTiming.overlayIn
    _ = AnimationTiming.overlayOut
    _ = AnimationTiming.cardDeal
    #expect(AnimationTiming.cardDealStagger == 0.060)
  }

  @Test func colorTokensExist() {
    // Compile-time existence check — if any symbol is missing, file won't compile.
    _ = BJSColors.surfaceBase
    _ = BJSColors.surfaceRaised
    _ = BJSColors.actionDark
    _ = BJSColors.surfaceOverlay
    _ = BJSColors.borderSubtle
    _ = BJSColors.borderOnOverlay
    _ = BJSColors.accentGold
    _ = BJSColors.actionLabel
    _ = BJSColors.watermarkInk
    _ = BJSColors.textPrimary
    _ = BJSColors.textSecondary
    _ = BJSColors.textOnOverlay
    _ = BJSColors.textOnOverlayMuted
    _ = BJSColors.feedbackCorrect
    _ = BJSColors.feedbackIncorrect
    _ = BJSColors.cardBackRed
  }

  @Test func typographyRolesExist() {
    _ = Typography.caption
    _ = Typography.body
    _ = Typography.title
  }

Register the new file in project.yml sources list if needed, then re-run XcodeGen.
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && xcodebuild test -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BJSTests/DesignTokenTests 2>&1 | tail -10 | grep -q "TEST SUCCEEDED\|** TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - File BJSTests/DesignTokenTests.swift exists
    - grep -c "@Test" BJSTests/DesignTokenTests.swift returns >= 6
    - All 16 BJSColors symbols referenced (grep counts match)
    - xcodebuild test for DesignTokenTests passes
  </acceptance_criteria>
  <done>DesignTokenTests compiles and passes. Every UI-SPEC token symbol is referenced at least once, proving existence; numeric values asserted where applicable.</done>
</task>

</tasks>

<verification>
- Full build: `xcodebuild build -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16'` succeeds
- Full test suite: `xcodebuild test -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16'` green (BJSCoreTests + BJSTests including new DesignTokenTests)
- grep for deleted symbols across BJS/ returns zero non-comment hits
</verification>

<success_criteria>
- Six token files rewritten verbatim against UI-SPEC Token Files block
- All existing views migrated and compile green
- DesignTokenTests in place and passing
- No regression in existing BJSCoreTests or BJSTests
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation-rebuild/07-01-SUMMARY.md`
</output>
