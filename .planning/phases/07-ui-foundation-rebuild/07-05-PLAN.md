---
phase: 07-ui-foundation-rebuild
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - BJS/Views/Trainer/SessionStartView.swift
  - BJS/Views/Trainer/SessionSummaryView.swift
  - BJS/Views/Common/StatsBarView.swift
  - BJS/Views/Common/SectionContainerView.swift
  - BJS/Views/Common/TrainingModeToggle.swift
autonomous: false
gap_closure: true
requirements: [UI-07-T1, UI-07-T2]
must_haves:
  truths:
    - "SessionStartView renders with dark BJS palette and BJS typography"
    - "SessionSummaryView renders with dark BJS palette and BJS typography"
    - "StatsBar, SectionContainer, TrainingModeToggle render with dark BJS palette"
    - "No file under BJS/Views uses Color(.system…) or Color(.separator)"
  artifacts:
    - path: BJS/Views/Trainer/SessionStartView.swift
      provides: "Dark-themed session start screen"
      contains: "BJSColors.surfaceBase"
    - path: BJS/Views/Trainer/SessionSummaryView.swift
      provides: "Dark-themed session summary screen"
      contains: "BJSColors.surfaceBase"
    - path: BJS/Views/Common/TrainingModeToggle.swift
      provides: "Toggle skinned with BJS tokens, no #warning"
  key_links:
    - from: "BJS/Views/Trainer/SessionStartView.swift"
      to: "BJS/Design/BJSColors.swift"
      via: "import + token references"
      pattern: "BJSColors\\.(surfaceBase|surfaceRaised|textPrimary|textSecondary)"
---

<objective>
Close gap-closure tests 7, 8, and 11 from 07-UAT.md. Wave 1's token migration was a false positive — five view files were never edited and still use Color(.system…). Replace every system color reference with BJSColors tokens, apply BJS typography, and remove the leftover placeholder #warning in TrainingModeToggle.

Purpose: Make the training-flow surfaces match the dark BJS theme so the app stops looking like a half-migrated demo.
Output: Five view files updated and a passing xcodebuild build.
</objective>

<execution_context>
@/Users/luke/BJS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/BJS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-foundation-rebuild/07-UAT.md
@.planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
@BJS/Design/BJSColors.swift
@BJS/Views/Trainer/SessionStartView.swift
@BJS/Views/Trainer/SessionSummaryView.swift
@BJS/Views/Common/StatsBarView.swift
@BJS/Views/Common/SectionContainerView.swift
@BJS/Views/Common/TrainingModeToggle.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate SessionStartView and SessionSummaryView to BJS tokens</name>
  <files>BJS/Views/Trainer/SessionStartView.swift, BJS/Views/Trainer/SessionSummaryView.swift</files>
  <action>
In SessionStartView.swift:
- Line 41: replace `Color(.systemGroupedBackground)` with `BJSColors.surfaceBase`
- Line 85: replace `Color(.secondarySystemBackground)` with `BJSColors.surfaceRaised`
- Replace any default `.foregroundColor(...)` / system text colors on labels with `BJSColors.textPrimary` for primary labels and `BJSColors.textSecondary` for secondary/helper labels.
- Replace default `.font(.title)`, `.font(.headline)`, `.font(.body)`, etc. with the BJS typography roles defined in BJSDesign (e.g. `BJSTypography.titleL`, `BJSTypography.body`) — match the role to the visual weight already in place.
- Do NOT change layout, spacing, or control structure. This is a token-swap pass only.

In SessionSummaryView.swift:
- Line 74: replace `Color(.systemGroupedBackground)` with `BJSColors.surfaceBase`
- Line 90: replace `Color(.secondarySystemBackground)` with `BJSColors.surfaceRaised`
- Apply BJS text/typography tokens to all stat labels (Hands Played, Accuracy, Errors, Best Streak) and the Mistakes list rows.

After editing, grep both files for `Color(.system` and `Color(.separator` — there must be zero matches.
  </action>
  <verify>
    <automated>! grep -nE "Color\(\\.system|Color\(\\.separator" BJS/Views/Trainer/SessionStartView.swift BJS/Views/Trainer/SessionSummaryView.swift</automated>
  </verify>
  <done>Both files reference only BJSColors / BJSTypography tokens; no system color or font references remain.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate StatsBarView, SectionContainerView, TrainingModeToggle to BJS tokens</name>
  <files>BJS/Views/Common/StatsBarView.swift, BJS/Views/Common/SectionContainerView.swift, BJS/Views/Common/TrainingModeToggle.swift</files>
  <action>
StatsBarView.swift:
- Lines 16, 19, 37: replace `Color(.secondarySystemBackground)` with `BJSColors.surfaceRaised`, and `Color(.separator)` with `BJSColors.divider` (or `BJSColors.surfaceRaised.opacity(...)` if no divider token exists — check BJSColors.swift first and pick the closest semantic token).
- Apply `BJSColors.textPrimary` / `textSecondary` to stat values and labels.

SectionContainerView.swift:
- Line 17: replace `Color(.secondarySystemBackground)` with `BJSColors.surfaceRaised`.

TrainingModeToggle.swift:
- Line 15: replace `Color(.separator)` with the appropriate BJS divider/border token.
- Line 28: replace `Color(.systemGray6)` with `BJSColors.surfaceRaised`.
- Apply `BJSColors.textPrimary` to the selected segment label and `BJSColors.textSecondary` to the unselected segment label.
- DELETE the leftover line: `#warning("Phase 7: TrainingModeToggle uses placeholder token — will be re-skinned in a later phase")`.

After editing, grep all three files for `Color(.system`, `Color(.separator`, and `#warning` — there must be zero matches.
  </action>
  <verify>
    <automated>! grep -nE "Color\(\\.system|Color\(\\.separator|#warning" BJS/Views/Common/StatsBarView.swift BJS/Views/Common/SectionContainerView.swift BJS/Views/Common/TrainingModeToggle.swift</automated>
  </verify>
  <done>All three Common views use only BJS tokens; placeholder #warning removed from TrainingModeToggle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Five view files migrated to BJS dark-theme tokens (SessionStart, SessionSummary, StatsBar, SectionContainer, TrainingModeToggle).</what-built>
  <how-to-verify>
1. Open Xcode and run `xcodebuild -project BJS.xcodeproj -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 15' build` from the repo root. Build MUST succeed.
2. Launch the app in the simulator. Navigate to:
   - Session start screen → confirm dark background, BJS fonts, no white surfaces.
   - Stats bar at the top of the trainer → dark surface, no light card.
   - Learn/Test toggle → dark surface, no system gray.
   - Complete a hand and reach Session Summary → dark background, BJS fonts.
3. Confirm zero visible white/system surfaces on any of the five screens.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- xcodebuild build succeeds with no warnings about deprecated tokens.
- Grep across the five files for `Color(.system|Color(.separator|#warning` returns zero matches.
- Visual confirmation by user that all five surfaces render dark.
</verification>

<success_criteria>
All five files use BJS tokens exclusively; build passes; visual UAT for tests 7, 8, 11 pass on re-run.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation-rebuild/07-05-SUMMARY.md`
</output>
