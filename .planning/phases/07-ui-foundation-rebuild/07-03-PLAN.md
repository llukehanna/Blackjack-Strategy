---
phase: 07-ui-foundation-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - BJS/Views/Trainer/TrainerView.swift
  - BJS/Views/Trainer/ActionButtonsView.swift
  - BJS/Views/Trainer/FeedbackOverlayView.swift
  - BJSTests/FeedbackOverlayTests.swift
autonomous: true
requirements: [UI-07-D2, UI-07-D3, UI-07-D4, UI-07-D5, UI-07-D7, UI-07-D8, UI-07-D9, UI-07-D10, UI-07-D11, UI-07-D12]

must_haves:
  truths:
    - "TrainerView renders with nav chrome (back chevron + SOS text button), dealer hand, watermark, player hand, table-edge arcs, and action dock — no inline StatsBar, no hand-total numerics"
    - "Action dock renders two rows of 88pt-tall cells with actionLabel blue icons/captions and hairline borderSubtle dividers"
    - "Feedback overlay is a bottom-anchored white card with straddling badge, heading, body with bolded action verbs, UNDERSTAND WHY + DEAL stacked buttons"
    - "Feedback heading/body/button copy matches the UI-SPEC Copywriting Contract verbatim"
  artifacts:
    - path: BJS/Views/Trainer/TrainerView.swift
      provides: "end-to-end TrainerView matching UI-SPEC layout contract"
    - path: BJS/Views/Trainer/ActionButtonsView.swift
      provides: "two-row 88pt action dock with hairline dividers"
    - path: BJS/Views/Trainer/FeedbackOverlayView.swift
      provides: "bottom card with straddling badge + 2 stacked buttons"
    - path: BJSTests/FeedbackOverlayTests.swift
      provides: "copy-string assertions for heading, body, button labels"
  key_links:
    - from: "TrainerView"
      to: "HandView (from Plan 02)"
      via: "dealer + player hand views"
      pattern: "HandView\\("
    - from: "TrainerView"
      to: "ActionButtonsView"
      via: "action dock child view"
      pattern: "ActionButtonsView\\("
    - from: "TrainerView"
      to: "FeedbackOverlayView"
      via: "overlay presentation"
      pattern: "FeedbackOverlayView\\("
    - from: "FeedbackOverlayView"
      to: "BJSColors.surfaceOverlay / textOnOverlay / actionDark"
      via: "token references"
      pattern: "BJSColors\\.(surfaceOverlay|textOnOverlay|textOnOverlayMuted|actionDark|feedbackCorrect|feedbackIncorrect|borderOnOverlay)"
---

<objective>
Delete and rebuild TrainerView, ActionButtonsView, and FeedbackOverlayView end-to-end against the UI-SPEC layout contract. No new gameplay behavior — same TrainerViewModel bindings, new presentation. Move StatsBar and End Session out of the play area into nav chrome (back chevron + SOS placeholder). Render table-edge arcs, watermark, two-row action dock, and bottom-card feedback overlay with straddling badge and both UNDERSTAND WHY + DEAL buttons.

Purpose: TrainerView is the visual contract for the whole app. Every UI-07-D# decision lands here. Delete-and-rewrite (per 07-RESEARCH Open Question 2) avoids drift from the reference.

Output: Three rewritten view files and one test file asserting copy strings verbatim.
</objective>

<execution_context>
@/Users/luke/BJS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/BJS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
@.planning/phases/07-ui-foundation-rebuild/07-01-SUMMARY.md
@.planning/phases/07-ui-foundation-rebuild/07-02-SUMMARY.md
@BJS/ViewModels/TrainerViewModel.swift
@BJS/Views/Trainer/HandView.swift
@BJS/Views/Trainer/CardView.swift

TrainerViewModel is untouched this phase. This plan only rebuilds presentation. Read TrainerViewModel to discover the exact published state (current player hand, dealer hand, hole card visibility, legal actions, feedback state, feedback correctness, correct action, user's attempted action).

Reference images: design-system/reference/IMG_7841.PNG (pre-decision), IMG_7843.PNG (incorrect feedback), IMG_7847.PNG (correct feedback — counting, structure still applies to copy pattern).

UI-SPEC Copywriting Contract (verbatim, uppercase, no synonyms):
  - Feedback heading correct: "Well played!"
  - Feedback heading incorrect: "Incorrect!"
  - Feedback body incorrect template: "In this situation {USER_ACTION} isn't the right move. You should have {CORRECT_ACTION}."
  - Feedback body correct template: "{ACTION} was the right move."
  - Action verbs bolded inline via AttributedString
  - Action labels: "STAND", "HIT", "SPLIT", "DOUBLE", "SURREN." (exact strings)
  - Primary CTA: "DEAL"
  - Secondary CTA: "UNDERSTAND WHY"
  - Brand watermark: "BJS" (placeholder)
  - End session confirmation: Title "End this session?" / Body "Your session results so far will be saved." / Confirm "End Session" / Cancel "Keep Playing"
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ActionButtonsView as two-row 88pt dock</name>
  <read_first>
    - BJS/Views/Trainer/ActionButtonsView.swift (current state, to be replaced)
    - BJS/ViewModels/TrainerViewModel.swift (to discover legal-actions API)
    - BJS/Design/BJSColors.swift
    - BJS/Design/Spacing.swift
    - BJS/Design/Typography.swift
    - .planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md (Action button contract section)
  </read_first>
  <behavior>
    - Row 1 always renders STAND + HIT
    - Row 2 renders SPLIT / DOUBLE / SURREN. only for legal actions (omitted entirely if none legal, like Phase 02.2)
    - Each cell is 88pt tall
    - Hairline borderSubtle (1pt) dividers between cells — vertical between cells in a row, horizontal between rows
    - Icon (SF Symbol) above caption label, VStack spacing Spacing.xs
    - Icon color + label color = BJSColors.actionLabel
    - Label uses Typography.caption .tracking(1.5), uppercase
  </behavior>
  <action>
Delete current BJS/Views/Trainer/ActionButtonsView.swift and rewrite wholesale. Structure:

  import SwiftUI

  struct ActionButtonsView: View {
      let canSplit: Bool
      let canDouble: Bool
      let canSurrender: Bool
      let onStand: () -> Void
      let onHit: () -> Void
      let onSplit: () -> Void
      let onDouble: () -> Void
      let onSurrender: () -> Void

      var body: some View {
          VStack(spacing: 0) {
              HStack(spacing: 0) {
                  ActionCell(icon: "hand.raised", label: "STAND", action: onStand)
                  Divider().frame(width: 1).background(BJSColors.borderSubtle)
                  ActionCell(icon: "plus", label: "HIT", action: onHit)
              }
              if secondaryRowVisible {
                  Divider().frame(height: 1).background(BJSColors.borderSubtle)
                  HStack(spacing: 0) {
                      if canSplit {
                          ActionCell(icon: "arrow.left.and.right", label: "SPLIT", action: onSplit)
                      }
                      if canSplit && (canDouble || canSurrender) {
                          Divider().frame(width: 1).background(BJSColors.borderSubtle)
                      }
                      if canDouble {
                          ActionCell(icon: "multiply.square", label: "DOUBLE", action: onDouble)
                      }
                      if canDouble && canSurrender {
                          Divider().frame(width: 1).background(BJSColors.borderSubtle)
                      }
                      if canSurrender {
                          ActionCell(icon: "flag", label: "SURREN.", action: onSurrender)
                      }
                  }
              }
          }
          .background(BJSColors.surfaceBase)
      }

      private var secondaryRowVisible: Bool { canSplit || canDouble || canSurrender }
  }

  private struct ActionCell: View {
      let icon: String
      let label: String
      let action: () -> Void

      var body: some View {
          Button(action: action) {
              VStack(spacing: Spacing.xs) {
                  Image(systemName: icon)
                      .font(.system(size: 24, weight: .regular))
                  Text(label)
                      .font(Typography.caption)
                      .tracking(1.5)
              }
              .foregroundStyle(BJSColors.actionLabel)
              .frame(maxWidth: .infinity)
              .frame(height: 88)
          }
      }
  }

Cross-check TrainerViewModel callsite to discover the actual legal-action property names and action method names; adapt ActionButtonsView parameters accordingly. Do NOT modify TrainerViewModel.
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && grep -c "height: 88" BJS/Views/Trainer/ActionButtonsView.swift | tr -d ' ' | grep -qv '^0$' && grep -c 'BJSColors\.actionLabel' BJS/Views/Trainer/ActionButtonsView.swift | tr -d ' ' | grep -qv '^0$' && xcodebuild build -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - grep "height: 88" BJS/Views/Trainer/ActionButtonsView.swift matches at least once
    - grep "\"STAND\"" BJS/Views/Trainer/ActionButtonsView.swift matches
    - grep "\"HIT\"" matches
    - grep "\"SPLIT\"" matches
    - grep "\"DOUBLE\"" matches
    - grep "\"SURREN.\"" matches
    - grep "BJSColors.actionLabel" matches
    - grep "BJSColors.borderSubtle" matches
    - grep "Typography.caption" matches
    - grep -c "\\.tracking(1.5)" returns >= 1
    - xcodebuild build succeeds
  </acceptance_criteria>
  <done>ActionButtonsView renders a two-row 88pt-tall dock with actionLabel blue content and hairline dividers, secondary row omitted when no secondary actions legal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite FeedbackOverlayView + FeedbackOverlayTests (copy contract)</name>
  <read_first>
    - BJS/Views/Trainer/FeedbackOverlayView.swift (current state, to be replaced)
    - BJS/ViewModels/TrainerViewModel.swift (feedback state: isCorrect, userAction, correctAction)
    - .planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md (Feedback Overlay Contract + Copywriting Contract sections)
    - BJSTests/CasinoPresetTests.swift (Swift Testing template)
  </read_first>
  <behavior>
    - FeedbackOverlayView renders a bottom-anchored white card
    - Status badge circle 48pt, half-overlapping top edge, centered horizontally
    - Correct state: green feedbackCorrect fill, white checkmark, heading "Well played!"
    - Incorrect state: red feedbackIncorrect fill, white xmark, heading "Incorrect!"
    - Body for incorrect: "In this situation {USER_ACTION} isn't the right move. You should have {CORRECT_ACTION}." (verbs bolded)
    - Body for correct: "{ACTION} was the right move." (verb bolded)
    - Two stacked buttons: UNDERSTAND WHY (outlined) above DEAL (dark fill)
    - UNDERSTAND WHY tap is a no-op placeholder (not removed — per UI-07-D7)
    - DEAL tap calls provided closure
    - FeedbackOverlayTests asserts: heading strings exact, button label strings exact, body template strings exact
  </behavior>
  <action>
Delete current BJS/Views/Trainer/FeedbackOverlayView.swift and rewrite wholesale:

  import SwiftUI

  struct FeedbackOverlayView: View {
      let isCorrect: Bool
      let userActionLabel: String      // e.g. "HIT"
      let correctActionLabel: String   // e.g. "DOUBLE"
      let onDeal: () -> Void

      static let headingCorrect = "Well played!"
      static let headingIncorrect = "Incorrect!"
      static let dealLabel = "DEAL"
      static let understandWhyLabel = "UNDERSTAND WHY"

      var body: some View {
          VStack(spacing: 0) {
              Spacer()
              ZStack(alignment: .top) {
                  RoundedRectangle(cornerRadius: CornerRadius.overlay)
                      .fill(BJSColors.surfaceOverlay)
                      .ignoresSafeArea(edges: .bottom)

                  VStack(spacing: Spacing.lg) {
                      Spacer().frame(height: Spacing.lg)  // below badge midpoint
                      Text(isCorrect ? Self.headingCorrect : Self.headingIncorrect)
                          .font(Typography.title)
                          .foregroundStyle(BJSColors.textOnOverlay)
                      Text(bodyAttributedString)
                          .font(Typography.body)
                          .foregroundStyle(BJSColors.textOnOverlayMuted)
                          .multilineTextAlignment(.center)
                          .padding(.horizontal, Spacing.lg)
                      VStack(spacing: Spacing.sm) {
                          UnderstandWhyButton()
                          DealButton(action: onDeal)
                      }
                      .padding(.horizontal, Spacing.lg)
                      .padding(.bottom, Spacing.xl)
                  }

                  badge
                      .offset(y: -24)  // half outside top edge
              }
              .fixedSize(horizontal: false, vertical: true)
          }
          .transition(.move(edge: .bottom).combined(with: .opacity))
      }

      private var badge: some View {
          ZStack {
              Circle()
                  .fill(isCorrect ? BJSColors.feedbackCorrect : BJSColors.feedbackIncorrect)
                  .frame(width: 48, height: 48)
              Image(systemName: isCorrect ? "checkmark" : "xmark")
                  .font(.system(size: 20, weight: .semibold))
                  .foregroundStyle(.white)
          }
      }

      private var bodyAttributedString: AttributedString {
          if isCorrect {
              var s = AttributedString("\(correctActionLabel) was the right move.")
              if let range = s.range(of: correctActionLabel) {
                  s[range].font = .system(size: 16, weight: .bold)
              }
              return s
          } else {
              var s = AttributedString("In this situation \(userActionLabel) isn't the right move. You should have \(correctActionLabel).")
              if let r1 = s.range(of: userActionLabel) {
                  s[r1].font = .system(size: 16, weight: .bold)
              }
              if let r2 = s.range(of: correctActionLabel) {
                  s[r2].font = .system(size: 16, weight: .bold)
              }
              return s
          }
      }
  }

  private struct UnderstandWhyButton: View {
      var body: some View {
          Button(action: { /* no-op placeholder — UI-07-D7 */ }) {
              HStack(spacing: Spacing.sm) {
                  Image(systemName: "questionmark.circle")
                  Text(FeedbackOverlayView.understandWhyLabel)
                      .font(Typography.caption)
                      .tracking(1.5)
              }
              .foregroundStyle(BJSColors.textOnOverlay)
              .frame(maxWidth: .infinity)
              .frame(height: 56)
              .background(
                  RoundedRectangle(cornerRadius: CornerRadius.overlayButton)
                      .fill(BJSColors.surfaceOverlay)
              )
              .overlay(
                  RoundedRectangle(cornerRadius: CornerRadius.overlayButton)
                      .stroke(BJSColors.borderOnOverlay, lineWidth: 1)
              )
          }
      }
  }

  private struct DealButton: View {
      let action: () -> Void
      var body: some View {
          Button(action: action) {
              HStack(spacing: Spacing.sm) {
                  Image(systemName: "square.on.square")
                  Text(FeedbackOverlayView.dealLabel)
                      .font(Typography.caption)
                      .tracking(1.5)
              }
              .foregroundStyle(.white)
              .frame(maxWidth: .infinity)
              .frame(height: 56)
              .background(
                  RoundedRectangle(cornerRadius: CornerRadius.overlayButton)
                      .fill(BJSColors.actionDark)
              )
          }
      }
  }

Create BJSTests/FeedbackOverlayTests.swift:

  import Testing
  @testable import BJS

  @Test func headingStringsExact() {
      #expect(FeedbackOverlayView.headingCorrect == "Well played!")
      #expect(FeedbackOverlayView.headingIncorrect == "Incorrect!")
  }

  @Test func buttonLabelsExact() {
      #expect(FeedbackOverlayView.dealLabel == "DEAL")
      #expect(FeedbackOverlayView.understandWhyLabel == "UNDERSTAND WHY")
  }

Add the test file to project.yml, re-run XcodeGen.
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && xcodebuild test -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BJSTests/FeedbackOverlayTests 2>&1 | tail -10 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - grep '"Well played!"' BJS/Views/Trainer/FeedbackOverlayView.swift matches once
    - grep '"Incorrect!"' matches once
    - grep '"DEAL"' matches once
    - grep '"UNDERSTAND WHY"' matches once
    - grep "isn't the right move. You should have" matches once
    - grep "BJSColors.feedbackCorrect" matches
    - grep "BJSColors.feedbackIncorrect" matches
    - grep "BJSColors.actionDark" matches
    - grep "BJSColors.borderOnOverlay" matches
    - grep "CornerRadius.overlay" matches (for both overlay and overlayButton)
    - FeedbackOverlayTests passes
  </acceptance_criteria>
  <done>FeedbackOverlayView renders bottom-anchored white card with straddling badge and two stacked buttons; copy strings match UI-SPEC verbatim; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite TrainerView end-to-end against UI-SPEC layout contract</name>
  <read_first>
    - BJS/Views/Trainer/TrainerView.swift (current state, to be replaced)
    - BJS/ViewModels/TrainerViewModel.swift (to discover state bindings: player hand, dealer hand, hole-card hidden, legal actions, feedback state, feedback action closures)
    - BJS/Views/Trainer/HandView.swift (from Plan 02)
    - BJS/Views/Trainer/ActionButtonsView.swift (from Task 1)
    - BJS/Views/Trainer/FeedbackOverlayView.swift (from Task 2)
    - .planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md (TrainerView Layout Contract section)
  </read_first>
  <action>
Delete current BJS/Views/Trainer/TrainerView.swift and rewrite wholesale. Preserve TrainerViewModel binding surface exactly — do NOT modify the view model. Discover the exact property names via the read_first pass.

Layout (top to bottom, matching UI-SPEC vertical zones diagram):

  ZStack(alignment: .bottom) {
      BJSColors.surfaceBase.ignoresSafeArea()

      VStack(spacing: 0) {
          // Nav chrome — 44pt
          HStack {
              Button(action: { showEndSessionConfirm = true }) {
                  Image(systemName: "chevron.left")
                      .font(.system(size: 16, weight: .semibold))
                      .foregroundStyle(BJSColors.textPrimary)
                      .frame(width: 32, height: 32)
                      .background(Circle().fill(BJSColors.surfaceRaised))
              }
              Spacer()
              Button(action: { /* SOS placeholder no-op */ }) {
                  Text("SOS")
                      .font(Typography.caption)
                      .tracking(1.5)
                      .foregroundStyle(BJSColors.actionLabel)
              }
          }
          .padding(.horizontal, Spacing.lg)
          .frame(height: 44)

          Spacer().frame(height: Spacing.xxl)  // 64pt breathing room

          // Dealer hand — 88pt cards, 30% overlap, centered, no labels or totals
          HandView(cards: vm.dealerCards, faceDownIndices: vm.dealerHoleHidden ? [1] : [], overlap: .dealer)

          Spacer().frame(height: Spacing.xl)  // 48pt

          // Watermark
          Text("BJS")
              .font(Typography.caption)
              .tracking(3)
              .foregroundStyle(BJSColors.watermarkInk.opacity(0.25))

          Spacer().frame(height: Spacing.xl)  // 48pt

          // Player hand — 88pt cards, 45% overlap
          HandView(cards: vm.playerCards, faceDownIndices: [], overlap: .player)

          Spacer(minLength: 0)

          // Table-edge arcs
          TableEdgeArcs()
              .frame(height: 60)

          // Action dock (hidden behind overlay when feedback shown)
          if !vm.isShowingFeedback {
              ActionButtonsView(
                  canSplit: vm.canSplit,
                  canDouble: vm.canDouble,
                  canSurrender: vm.canSurrender,
                  onStand: vm.stand,
                  onHit: vm.hit,
                  onSplit: vm.split,
                  onDouble: vm.double,
                  onSurrender: vm.surrender
              )
          }
      }

      if vm.isShowingFeedback {
          FeedbackOverlayView(
              isCorrect: vm.lastActionWasCorrect,
              userActionLabel: vm.lastUserActionLabel,
              correctActionLabel: vm.lastCorrectActionLabel,
              onDeal: vm.deal
          )
          .animation(AnimationTiming.overlayIn, value: vm.isShowingFeedback)
      }
  }
  .alert("End this session?", isPresented: $showEndSessionConfirm) {
      Button("End Session", role: .destructive) { vm.endSession() }
      Button("Keep Playing", role: .cancel) {}
  } message: {
      Text("Your session results so far will be saved.")
  }

Also create a private TableEdgeArcs View: two large ellipses stroked with BJSColors.accentGold at 1pt, clipped at screen edges (use .stroke and an offset/frame pattern; exact geometry is approximate per UI-SPEC "observed: false — inferred").

Adapt property names in the VStack body to the ACTUAL TrainerViewModel API discovered in read_first. If any name is wrong, fix it — do not modify the view model. If the view model uses different feedback state names (e.g. `feedback: FeedbackState?` enum), map appropriately.

StatsBarView is NOT rendered anywhere inside TrainerView (UI-07-D2). If SessionStartView or another screen still uses StatsBarView that's fine — out of scope here.

No hand-total numerics anywhere (UI-07-D10). No "DEALER" or "YOU" labels.

Add Xcode Previews for three states: pre-decision, awaiting-decision-after-hit, feedback-shown.
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && grep -c "StatsBarView" BJS/Views/Trainer/TrainerView.swift | tr -d ' ' | grep -q '^0$' && grep -c "HandView" BJS/Views/Trainer/TrainerView.swift | tr -d ' ' | grep -qv '^0$' && xcodebuild test -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - grep "StatsBarView" BJS/Views/Trainer/TrainerView.swift returns zero matches (UI-07-D2)
    - grep "\"DEALER\"\\|\"YOU\"" BJS/Views/Trainer/TrainerView.swift returns zero matches (UI-07-D10)
    - grep "HandView" matches at least twice (dealer + player)
    - grep "FeedbackOverlayView" matches
    - grep "ActionButtonsView" matches
    - grep "\"SOS\"" matches
    - grep "\"BJS\"" matches (watermark)
    - grep "BJSColors.accentGold" matches (table-edge arcs)
    - grep "chevron.left" matches
    - grep "End this session?" matches
    - grep "Your session results so far will be saved." matches
    - grep "End Session" matches
    - grep "Keep Playing" matches
    - Full test suite green
    - Xcode Previews render without crash (manual visual check deferred to Plan 04)
  </acceptance_criteria>
  <done>TrainerView renders end-to-end against UI-SPEC layout contract with nav chrome, no inline StatsBar, no hand totals, dealer/player hands, watermark, table-edge arcs, two-row action dock, and bottom-card feedback overlay. TrainerViewModel is unchanged.</done>
</task>

</tasks>

<verification>
- Full test suite green
- Project builds and TrainerView Previews render in Xcode for all three states
- grep confirms no StatsBarView and no hand-total numerics inside TrainerView
- All UI-07-D copywriting strings present verbatim
</verification>

<success_criteria>
- TrainerView, ActionButtonsView, FeedbackOverlayView rewritten from scratch
- FeedbackOverlayTests passing
- No regression in BJSCoreTests or BJSTests
- TrainerViewModel unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation-rebuild/07-03-SUMMARY.md`
</output>
