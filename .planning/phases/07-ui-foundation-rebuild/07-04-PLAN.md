---
phase: 07-ui-foundation-rebuild
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md
autonomous: false
requirements: [UI-07-D1, UI-07-D2, UI-07-D3, UI-07-D4, UI-07-D5, UI-07-D6, UI-07-D7, UI-07-D8, UI-07-D9, UI-07-D10, UI-07-D11, UI-07-D12, UI-07-D13, UI-07-D14, UI-07-D15]

must_haves:
  truths:
    - "Full test suite (BJSCoreTests + BJSTests) passes green"
    - "TrainerView Xcode Previews render for pre-decision, awaiting-decision, and feedback-shown states without crash"
    - "Visual side-by-side against design-system/reference/IMG_7841.PNG and IMG_7843.PNG is user-approved"
  artifacts:
    - path: .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md
      provides: "screenshot paths, test-suite result, per-decision UI-07-D# sign-off, user approval signal"
  key_links:
    - from: "07-VERIFICATION.md"
      to: "design-system/reference/IMG_7841.PNG"
      via: "side-by-side visual comparison"
      pattern: "IMG_7841"
---

<objective>
Final verification gate for Phase 7. Run full test suite, capture TrainerView Preview screenshots in the three canonical states, lay them side-by-side against the reference images, and obtain explicit user approval against every UI-07-D decision.

Purpose: Phase 7 is a visual contract phase. Automated tests cover token values, asset resolution, and copy strings, but pixel fidelity requires a human eye. This plan is the formal sign-off gate.

Output: 07-VERIFICATION.md with test-suite output, screenshot file paths, per-decision sign-off checklist, and user approval marker.
</objective>

<execution_context>
@/Users/luke/BJS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/BJS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
@.planning/phases/07-ui-foundation-rebuild/07-01-SUMMARY.md
@.planning/phases/07-ui-foundation-rebuild/07-02-SUMMARY.md
@.planning/phases/07-ui-foundation-rebuild/07-03-SUMMARY.md

Reference images live in design-system/reference/:
  - IMG_7841.PNG — TrainerView pre-decision (primary)
  - IMG_7843.PNG — TrainerView feedback overlay incorrect
  - IMG_7847.PNG — feedback overlay correct (counting variant, structure applies)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and capture TrainerView Preview screenshots</name>
  <read_first>
    - BJS/Views/Trainer/TrainerView.swift (confirm Preview providers exist for three states)
  </read_first>
  <action>
Run the full test suite:
  xcodebuild test -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tee /tmp/bjs-phase7-test.log

Capture the result (pass/fail count, any red tests). Save log path for VERIFICATION.md.

Capture TrainerView Previews as PNGs. Two options — attempt in order:

  Option A (Xcode Preview export via xcrun simctl / scripted build):
    xcodebuild build-for-testing -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16'
    xcrun simctl boot 'iPhone 16' 2>/dev/null || true
    (Launch BJS, navigate to TrainerView, invoke each state via debug affordances if available)
    xcrun simctl io booted screenshot /tmp/bjs-phase7-pre.png
    xcrun simctl io booted screenshot /tmp/bjs-phase7-feedback.png

  Option B (Xcode Preview export is unreliable from CLI): skip automated capture and record "manual capture required" in VERIFICATION.md, defer actual screenshot capture to Task 2 human step.

Create .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md with:
  - Date
  - Test suite result (parsed from /tmp/bjs-phase7-test.log)
  - Screenshot file paths (or "pending manual capture")
  - Reference image paths (design-system/reference/IMG_7841.PNG, IMG_7843.PNG)
  - Per-decision checklist: 15 rows, one per UI-07-D1 through UI-07-D15, each with a checkbox and a one-line "how to verify" pointer
  - Token rewrite blast-radius status: "No deleted-symbol references remain (Plan 01 migration complete)"
  - Approval line: "Approved by: [pending]"
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && test -f .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md && grep -c "UI-07-D" .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md | tr -d ' ' | awk '{if ($1 >= 15) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md exists
    - File contains all 15 UI-07-D# checkboxes
    - File records test suite pass/fail result from full run
    - File references IMG_7841.PNG and IMG_7843.PNG as comparison targets
  </acceptance_criteria>
  <done>Test suite run recorded, VERIFICATION.md scaffolded with per-decision checklist and references.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual approval gate</name>
  <read_first>
    - .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md
    - .planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md (Decisions Locked table)
  </read_first>
  <what-built>
    Plans 01-03 delivered: rewritten token system, imported public-domain SVG card deck, rebuilt CardView/HandView with differentiated overlap, rebuilt TrainerView/ActionButtonsView/FeedbackOverlayView end-to-end against UI-SPEC. All automated tests green (DesignTokenTests, CardAssetTests, CardViewTests, HandViewTests, FeedbackOverlayTests, plus existing BJSCoreTests/BJSTests suite).
  </what-built>
  <how-to-verify>
    1. Open BJS.xcodeproj in Xcode 26
    2. Open BJS/Views/Trainer/TrainerView.swift — confirm Preview canvas renders all three states without crash
    3. Side-by-side the pre-decision Preview against design-system/reference/IMG_7841.PNG. Confirm:
       - Dark background matches (#0A0A0E near-black)
       - Dealer hand is one face-up card + red card back, overlap ~30%
       - Watermark "BJS" is faint, cool teal cast, centered between hands
       - Player hand is two face-up cards, overlap ~45%
       - NO hand-total numerics visible
       - NO "DEALER" / "YOU" labels
       - Top-left: back chevron in small dark circle
       - Top-right: "SOS" text (blue-grey), NOT a book icon
       - Table-edge gold arcs visible at left/right above action dock
       - Action dock: row 1 STAND/HIT, row 2 SPLIT/DOUBLE/SURREN. (when legal), cells ~88pt tall, blue icons + captions
    4. Side-by-side the feedback-incorrect Preview against IMG_7843.PNG. Confirm:
       - White card covers action dock (not the hands)
       - Red X badge straddles top edge of white card
       - Heading "Incorrect!" in bold dark text
       - Body text with bolded action verbs
       - Two stacked buttons: UNDERSTAND WHY (outlined) above DEAL (dark)
       - Hands above feedback card remain fully visible
    5. Tick every UI-07-D1 through UI-07-D15 checkbox in 07-VERIFICATION.md
    6. Run TrainerView in iPhone 16 simulator, play one hand end-to-end: deal, decide, receive feedback, tap DEAL, deal next hand. Confirm no regressions in TrainerViewModel behavior.
    7. Record user approval in 07-VERIFICATION.md "Approved by" line with date.
  </how-to-verify>
  <action>
    Claude presents the verification checklist to the user with explicit references to IMG_7841.PNG and IMG_7843.PNG side-by-side paths. Claude blocks until the user types "approved" (phase complete) or lists specific gaps (triggers /gsd:plan-phase 7 --gaps).

    If the user flags gaps, Claude captures them verbatim in 07-VERIFICATION.md under a "Gaps" section for the --gaps closure pass.
  </action>
  <resume-signal>approved OR list specific gaps</resume-signal>
  <verify>
    <automated>grep -q "^Approved by: " .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md && ! grep -q "^Approved by: \[pending\]" .planning/phases/07-ui-foundation-rebuild/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every UI-07-D# checkbox in 07-VERIFICATION.md is ticked OR a gap is explicitly recorded
    - "Approved by:" line is filled in (not "[pending]")
    - If gaps exist, they are captured verbatim for a follow-up --gaps pass
  </acceptance_criteria>
  <done>User has explicitly approved the rebuild against UI-SPEC decisions, or recorded gaps for a gap-closure plan.</done>
</task>

</tasks>

<verification>
- Full xcodebuild test suite green
- 07-VERIFICATION.md complete with all 15 UI-07-D# sign-offs
- User approval line populated
</verification>

<success_criteria>
- Test suite green
- Visual side-by-side against IMG_7841/IMG_7843 approved by user
- Phase 7 formally complete and ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation-rebuild/07-04-SUMMARY.md`
</output>
