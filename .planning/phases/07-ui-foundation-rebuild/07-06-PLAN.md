---
phase: 07-ui-foundation-rebuild
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - BJS/Views/Trainer/FeedbackOverlayView.swift
autonomous: false
gap_closure: true
requirements: [UI-07-D5, UI-07-D6]
must_haves:
  truths:
    - "Feedback card is bottom-anchored and only covers the action-dock region, not the full screen"
    - "Play area above the card remains visible after a decision"
    - "X badge visually straddles the top edge of the white feedback card"
  artifacts:
    - path: BJS/Views/Trainer/FeedbackOverlayView.swift
      provides: "Bottom-anchored feedback card with badge straddling top edge"
      contains: "fixedSize"
  key_links:
    - from: "BJS/Views/Trainer/TrainerView.swift"
      to: "BJS/Views/Trainer/FeedbackOverlayView.swift"
      via: "ZStack(alignment: .bottom) overlay"
      pattern: "FeedbackOverlayView"
---

<objective>
Close UAT tests 4 and 5. The feedback overlay currently fills the entire screen because its outer VStack + Spacer expands to TrainerView's full ZStack height. The card surface color is correct (white per UI-SPEC, surfaceOverlay = #FFFFFF) — only the wrapping layout is wrong. After the structural fix, the X badge will naturally straddle the top edge of the now-correctly-sized card.

Purpose: Restore the bottom-anchored feedback affordance so users can still see their hand while reading feedback.
Output: FeedbackOverlayView.swift edited and visually verified.
</objective>

<execution_context>
@/Users/luke/BJS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/BJS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-foundation-rebuild/07-UAT.md
@.planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
@BJS/Views/Trainer/FeedbackOverlayView.swift
@BJS/Views/Trainer/TrainerView.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove wrapping VStack/Spacer; reduce internal top clearance</name>
  <files>BJS/Views/Trainer/FeedbackOverlayView.swift</files>
  <action>
Per UAT gap diagnosis (lines 32–62):

1. Locate the outer `VStack { Spacer(minLength: 0); ZStack(...) { ... card ... } }` that is the body of FeedbackOverlayView.
2. DELETE the wrapping VStack and the Spacer(minLength: 0). Make the inner `ZStack(alignment: .top) { ... }` (the one containing the white card and the badge) the root view returned by `body`.
3. Keep all existing modifiers on the ZStack: `.fixedSize(horizontal: false, vertical: true)`, padding, background fill (white = `BJSColors.surfaceOverlay`), corner radius, shadow, badge `.offset(y: -24)`. Do NOT change card color — UI-SPEC requires it to be WHITE.
4. Inside the card content stack, reduce the top clearance: change the leading `Spacer().frame(height: Spacing.lg)` (or the equivalent first spacer below the badge anchor) to `Spacer().frame(height: Spacing.xs)` so total top clearance drops from ~40pt to ~8pt. The heading should sit flush below the badge with no dead-zone.
5. Confirm no other layout modifiers were added — TrainerView already wraps this view in a `ZStack(alignment: .bottom)` overlay, which together with `.fixedSize(vertical: true)` will pin the card to the bottom and let the play area show through above it.

After editing, grep the file for `Spacer(minLength: 0)` — there must be zero matches in the body root.
  </action>
  <verify>
    <automated>! grep -n "Spacer(minLength: 0)" BJS/Views/Trainer/FeedbackOverlayView.swift</automated>
  </verify>
  <done>Outer VStack/Spacer removed; ZStack(card) is the body root; internal top clearance reduced to ~8pt; card remains white.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>FeedbackOverlayView restructured to be a self-sizing bottom-anchored card.</what-built>
  <how-to-verify>
1. Run `xcodebuild -project BJS.xcodeproj -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 15' build`. Build MUST succeed.
2. Run `xcodebuild test -project BJS.xcodeproj -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:BJSTests/FeedbackOverlayTests`. The existing FeedbackOverlayTests should still pass (this plan does not change the button label).
3. Launch app in simulator. In the trainer, make a decision (HIT or STAND on a hand where the result resolves) and confirm:
   - The white feedback card sits at the BOTTOM of the screen, covering only the dock region.
   - The dealer hand, player hand, and felt remain visible above the card.
   - The red X (or green check) badge straddles the top edge of the white card — half above, half below.
   - There is no large gap between the badge and the heading text.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- xcodebuild build + FeedbackOverlayTests pass.
- Visual confirmation that card is bottom-anchored, white, and badge straddles its top edge.
</verification>

<success_criteria>
UAT tests 4 and 5 re-run as pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation-rebuild/07-06-SUMMARY.md`
</output>
