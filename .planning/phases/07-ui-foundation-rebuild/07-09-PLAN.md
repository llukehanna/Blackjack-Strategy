---
phase: 07-ui-foundation-rebuild
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - BJS/Views/Trainer/CardView.swift
  - BJS/Views/Trainer/HandView.swift
autonomous: false
gap_closure: true
requirements: [UI-07-D11]
must_haves:
  truths:
    - "Each rendered card has the exact pixel width passed by HandView"
    - "Dealer cards overlap more tightly than player cards (visibly differentiated)"
    - "Each card remains 70–80% visible (~20–30% overlap), not stacked on top of each other"
  artifacts:
    - path: BJS/Views/Trainer/CardView.swift
      provides: "CardView with explicit width parameter applied to its Image"
      contains: "let width"
    - path: BJS/Views/Trainer/HandView.swift
      provides: "HandView passing cardWidth into CardView, no wrapping-Group .frame"
  key_links:
    - from: "HandView"
      to: "CardView"
      via: "explicit width parameter"
      pattern: "CardView\\(.*width:"
---

<objective>
Close UAT test 2. Cards currently render wider than 88pt because CardView uses `.aspectRatio(5/7, .fit)` with no explicit `.frame(width:)` and HandView's `.frame(width: cardWidth)` is applied to a Group wrapper instead of the inner Image. The negative spacing was calculated against 88pt but cards render larger, so the dealer/player overlap differentiation (0.30 vs 0.45) is swamped.

Purpose: Restore the visible difference between dealer and player hand layouts so users can quickly read both hands.
Output: CardView and HandView edited; visible card layout matches UI-SPEC D11.
</objective>

<execution_context>
@/Users/luke/BJS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/BJS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-foundation-rebuild/07-UAT.md
@.planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
@BJS/Views/Trainer/CardView.swift
@BJS/Views/Trainer/HandView.swift
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add explicit width parameter to CardView</name>
  <files>BJS/Views/Trainer/CardView.swift</files>
  <action>
1. Add a stored property to CardView: `let width: CGFloat`.
2. On the inner card Image (and the card-back image branch, if separate), apply `.frame(width: width, height: width * 7.0/5.0)` directly to the Image (NOT to an outer wrapper).
3. Remove the existing `.aspectRatio(5.0/7.0, contentMode: .fit)` modifier on line 26 — the explicit frame replaces it. Keep `.scaledToFit()` or `.resizable()` as needed for the Image to fill the explicit frame correctly.
4. Update the SwiftUI preview at the bottom of the file to pass an explicit width (e.g. `CardView(card: ..., width: 88)`).
  </action>
  <verify>
    <automated>grep -nE "let width: CGFloat" BJS/Views/Trainer/CardView.swift && ! grep -n "aspectRatio(5" BJS/Views/Trainer/CardView.swift</automated>
  </verify>
  <done>CardView requires an explicit width; aspectRatio modifier removed; preview updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pass cardWidth into CardView from HandView; remove wrapping-Group frame</name>
  <files>BJS/Views/Trainer/HandView.swift</files>
  <action>
1. Locate line 29 where the Group / wrapper has `.frame(width: cardWidth)`. DELETE that `.frame(width: cardWidth)` from the Group wrapper.
2. Update every CardView construction in HandView to pass `width: cardWidth` explicitly:
   ```swift
   CardView(card: card, width: cardWidth)
   ```
   (and for the face-down case if a separate constructor is used).
3. Confirm the negative-spacing calculation still uses `cardWidth` and the overlap fractions per UI-SPEC D11 (dealer 0.30, player 0.45). Do NOT change these constants — the bug was that the rendered width didn't match `cardWidth`, not that the constants were wrong.
4. If HandView fixes a `cardWidth` constant somewhere in the file, ensure it stays at 88pt (or whatever UI-SPEC D11 specifies).
  </action>
  <verify>
    <automated>grep -nE "CardView\\(.*width: cardWidth" BJS/Views/Trainer/HandView.swift && ! grep -n "\\.frame(width: cardWidth)" BJS/Views/Trainer/HandView.swift</automated>
  </verify>
  <done>HandView passes cardWidth into CardView; wrapping Group .frame removed; overlap constants unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CardView gains explicit width; HandView wires it through; rendered card width now matches the value the negative-spacing math was based on.</what-built>
  <how-to-verify>
1. Run `xcodebuild build` — must succeed.
2. Run `xcodebuild test` — all existing tests must pass.
3. Launch app. Deal 5 different hands and confirm:
   - Dealer hand: cards overlap tightly, dealer hole-card back covers ~30% of the up-card (not 70%).
   - Player hand: cards overlap less than the dealer's, each card is ~70–80% visible.
   - The two hands look visibly different in their fanning width.
4. Try a hand with 3+ player cards (after a HIT) and confirm the player hand still fans correctly without overflowing.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- xcodebuild build + test pass.
- Manual visual confirmation that dealer and player hands have visibly different overlap.
</verification>

<success_criteria>
UAT test 2 re-runs as pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation-rebuild/07-09-SUMMARY.md`
</output>
