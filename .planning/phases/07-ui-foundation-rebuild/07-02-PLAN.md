---
phase: 07-ui-foundation-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - BJS/Resources/Cards/Cards.xcassets
  - BJS/Resources/Cards/ATTRIBUTION.md
  - BJS/Views/Trainer/CardView.swift
  - BJS/Views/Trainer/HandView.swift
  - BJSTests/CardAssetTests.swift
  - BJSTests/CardViewTests.swift
  - BJSTests/HandViewTests.swift
  - project.yml
autonomous: false
requirements: [UI-07-D6, UI-07-D13, UI-07-D15]

must_haves:
  truths:
    - "52 face-card SVGs plus 1 card-back SVG resolve via UIImage(named:) from the app bundle"
    - "CardView renders real SVG art (not SF Symbols) for any (rank, suit) and for faceDown"
    - "HandView composes cards using HandOverlap.dealer (0.30) and HandOverlap.player (0.45) with negative HStack spacing"
    - "License for the card deck is positively verified as public-domain/CC0 and recorded in ATTRIBUTION.md"
  artifacts:
    - path: BJS/Resources/Cards/Cards.xcassets
      provides: "52 face SVGs + card_back SVG with Preserve Vector Data"
    - path: BJS/Resources/Cards/ATTRIBUTION.md
      provides: "source URL, license URL, verification date, license text excerpt"
    - path: BJS/Views/Trainer/CardView.swift
      provides: "CardView(card:) / CardView(faceDown:) rendering SVG assets"
    - path: BJS/Views/Trainer/HandView.swift
      provides: "HandOverlap enum + HandView with negative HStack spacing"
    - path: BJSTests/CardAssetTests.swift
      provides: "loop over all 52 (rank, suit) + card_back, assert UIImage(named:) non-nil"
  key_links:
    - from: "BJS/Views/Trainer/CardView.swift"
      to: "Cards.xcassets"
      via: "Image(\"{rank}_of_{suit}\") lookup"
      pattern: "Image\\(\"[a-z0-9_]+\"\\)"
    - from: "BJS/Views/Trainer/HandView.swift"
      to: "CardView"
      via: "HStack(spacing: -width * overlap.rawValue)"
      pattern: "HStack\\(spacing:"
---

<objective>
Import a verified public-domain SVG playing-card deck into the BJS asset catalog, rebuild CardView to render real card art (not SF Symbols), rebuild HandView with differentiated dealer/player overlap, and add Wave-0 tests that verify every asset resolves and the HandOverlap values match UI-SPEC.

Purpose: Real card art is the single most visually impactful element of the Phase 7 rebuild. UI-07-D6 requires SVG assets; UI-07-D13 requires 30%/45% differentiated overlaps; UI-07-D15 locks the card-back red value (owned by the token file from Plan 01 and applied visually in the SVG back asset).

Output: 53 SVGs in Cards.xcassets with Preserve Vector Data, ATTRIBUTION.md with verified license, rewritten CardView + HandView, three test files (CardAssetTests, CardViewTests, HandViewTests).
</objective>

<execution_context>
@/Users/luke/BJS/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luke/BJS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md
@.planning/phases/07-ui-foundation-rebuild/07-RESEARCH.md
@.planning/phases/07-ui-foundation-rebuild/07-01-SUMMARY.md

Card rendering contract (from UI-SPEC):
  - Aspect 5:7, default width 88pt
  - CornerRadius.card (8pt) applied as clip
  - Elevation.card applied as .shadow
  - Asset naming: lowercase {rank}_of_{suit} (ace_of_spades, 10_of_hearts, king_of_clubs, etc.)
  - Card back asset: "card_back"
  - No size parameter on CardView — width fixed by parent HandView layout
  - HandOverlap enum with .dealer=0.30 and .player=0.45 (raw CGFloat)
  - Negative HStack spacing pattern for overlap

BJSCore Card type (pure Swift, no SwiftUI) has Rank + Suit enums. CardView must live in the app module (NOT BJSCore) and map Card -> asset name.

Pitfall 2 (07-RESEARCH.md): file-name mismatch between fork and CardView lookup silently yields blank Images. CardAssetTests iterates every (rank, suit) and asserts non-nil UIImage.

Pitfall 3: license drift. MANDATORY license verification against source before commit; abort if not positively CC0/public domain.

Pitfall 4: do NOT apply .clipped() to HandView (negative spacing clips cards at container edges).
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Fetch and license-verify public-domain SVG deck</name>
  <read_first>
    - .planning/phases/07-ui-foundation-rebuild/07-RESEARCH.md (Pitfall 3, Sources section)
    - .planning/phases/07-ui-foundation-rebuild/07-UI-SPEC.md (Card Rendering Contract)
  </read_first>
  <what-built>
Claude will attempt to git-clone https://github.com/notpeter/Vector-Playing-Cards (maintained fork of Byron Knoll public-domain SVG deck) into a temp directory, read the LICENSE file, cross-reference against Byron Knoll's original blog post (http://byronknoll.blogspot.com/2011/03/vector-playing-cards.html), and spot-check at least one face-card SVG for embedded copyright metadata.
  </what-built>
  <how-to-verify>
    1. Confirm repo clones successfully (or user provides alternate verified CC0 source)
    2. Confirm LICENSE file explicitly states public domain / CC0 / Unlicense
    3. Confirm original blog post still declares public-domain release
    4. Confirm spot-checked SVG has no conflicting copyright string in metadata
    5. User approves OR specifies an alternate source (saulspatz/SVGCards, Tek Eye, etc.)
  </how-to-verify>
  <action>
Claude runs:
  git clone --depth 1 https://github.com/notpeter/Vector-Playing-Cards /tmp/bjs-cards
  cat /tmp/bjs-cards/LICENSE  (or LICENSE.md / README.md license section)
  curl -s http://byronknoll.blogspot.com/2011/03/vector-playing-cards.html | grep -i "public domain"
  grep -i "copyright\|license" /tmp/bjs-cards/SVG-cards-1.3/king_of_spades.svg

Claude reports findings to the user. Checkpoint blocks until user confirms license is positively CC0/public-domain OR chooses an alternate source.

Resume signal: user types "approved: {source}" or "use alternate: {url}"
  </action>
  <resume-signal>approved: {source_url}</resume-signal>
  <verify>
    <automated>test -d /tmp/bjs-cards && test -f /tmp/bjs-cards/LICENSE</automated>
  </verify>
  <acceptance_criteria>
    - User has explicitly approved a specific source URL
    - LICENSE file content confirmed public-domain/CC0
    - Original blog post or equivalent authority confirms public-domain release
  </acceptance_criteria>
  <done>User approval recorded. Source URL and license URL captured for ATTRIBUTION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Import 52 faces + card_back into Cards.xcassets, write ATTRIBUTION.md</name>
  <read_first>
    - BJS/Resources/ (directory listing to confirm path convention)
    - project.yml (to verify how resources are declared for XcodeGen)
    - /tmp/bjs-cards/ (cloned source from Task 1)
  </read_first>
  <action>
Create BJS/Resources/Cards/Cards.xcassets/ with an Asset Catalog structure. For each of the 52 face cards and 1 card back:

  1. Copy the SVG from /tmp/bjs-cards/ into Cards.xcassets/{name}.imageset/{name}.svg
  2. Create Contents.json in each imageset with:
     {
       "images" : [ { "filename" : "{name}.svg", "idiom" : "universal" } ],
       "info" : { "author" : "xcode", "version" : 1 },
       "properties" : { "preserves-vector-representation" : true, "template-rendering-intent" : "original" }
     }

Naming: exactly match `{rank}_of_{suit}` lowercase. ranks: ace, 2, 3, 4, 5, 6, 7, 8, 9, 10, jack, queen, king. suits: clubs, diamonds, hearts, spades. Card back filename: `card_back`.

If the source fork uses different filenames (e.g., `jack_of_spades2.svg` for the alt face), pick ONE variant per card and rename to the canonical form above. Record variant choices in ATTRIBUTION.md.

Create BJS/Resources/Cards/ATTRIBUTION.md with:
  - Source repository URL (from Task 1 approval)
  - Original author attribution (Byron Knoll or whoever user approved)
  - License URL and verification date (today)
  - Excerpt of license statement (copy-paste from the LICENSE file)
  - Any file renames applied
  - Card-back choice note: "Using {source}'s {filename} as card_back; visual field tinted via CardView shadow + frame (cardBackRed token applied in Plan 03 TrainerView if needed)"

Update project.yml to include BJS/Resources/Cards in resources sources if not already covered by a wildcard. Run XcodeGen.
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && ls BJS/Resources/Cards/Cards.xcassets | grep -c "\.imageset$" | tr -d ' ' | grep -q '^53$' && test -f BJS/Resources/Cards/ATTRIBUTION.md && grep -qi "public domain\|cc0\|unlicense" BJS/Resources/Cards/ATTRIBUTION.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 53 .imageset directories inside Cards.xcassets (52 faces + card_back)
    - Every imageset contains a Contents.json with "preserves-vector-representation": true
    - ATTRIBUTION.md exists, references the approved source URL, and contains a license excerpt
    - XcodeGen re-run succeeds; project builds green (may still use placeholder CardView from Plan 01 migration)
    - grep -l "ace_of_spades.svg" BJS/Resources/Cards/Cards.xcassets/ace_of_spades.imageset/ finds the file
  </acceptance_criteria>
  <done>All 53 SVG assets present with vector preservation enabled, ATTRIBUTION.md records verified license, project still builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite CardView + HandView against SVG assets, add CardAssetTests / CardViewTests / HandViewTests</name>
  <read_first>
    - BJS/Views/Trainer/CardView.swift (current placeholder after Plan 01 migration)
    - BJS/Views/Trainer/HandView.swift
    - BJS/Design/CornerRadius.swift
    - BJS/Design/Elevation.swift
    - BJS/Design/BJSColors.swift
    - BJSCore sources for Card / Rank / Suit type definitions (find via grep if path unknown)
    - BJSTests/CasinoPresetTests.swift (Swift Testing template)
  </read_first>
  <behavior>
    - CardAssetTests.allFaceCardsResolve: for every (Rank, Suit), UIImage(named: "{rank}_of_{suit}") is non-nil
    - CardAssetTests.cardBackResolves: UIImage(named: "card_back") non-nil
    - CardViewTests.renderAllCardsNoCrash: for every (Rank, Suit), CardView(card: card, faceDown: false) hosts in a UIHostingController and reports non-zero intrinsic bounds
    - CardViewTests.faceDownRenders: CardView(card: nil, faceDown: true) renders without crash
    - HandViewTests.overlapValues: HandOverlap.dealer.rawValue == 0.30, HandOverlap.player.rawValue == 0.45
  </behavior>
  <action>
Rewrite BJS/Views/Trainer/CardView.swift wholesale:

  import SwiftUI
  import BJSCore

  struct CardView: View {
      let card: Card?
      let faceDown: Bool

      init(card: Card) { self.card = card; self.faceDown = false }
      init(faceDown: Bool) { self.card = nil; self.faceDown = faceDown }

      var body: some View {
          Image(assetName)
              .resizable()
              .aspectRatio(5.0/7.0, contentMode: .fit)
              .clipShape(RoundedRectangle(cornerRadius: CornerRadius.card))
              .shadow(
                  color: Elevation.card.color,
                  radius: Elevation.card.radius,
                  x: Elevation.card.x,
                  y: Elevation.card.y
              )
              .accessibilityLabel(accessibilityText)
      }

      private var assetName: String {
          if faceDown || card == nil { return "card_back" }
          return "\(rankName(card!.rank))_of_\(suitName(card!.suit))"
      }

      private func rankName(_ r: Rank) -> String {
          switch r {
          case .ace: return "ace"
          case .two: return "2"
          case .three: return "3"
          case .four: return "4"
          case .five: return "5"
          case .six: return "6"
          case .seven: return "7"
          case .eight: return "8"
          case .nine: return "9"
          case .ten: return "10"
          case .jack: return "jack"
          case .queen: return "queen"
          case .king: return "king"
          }
      }

      private func suitName(_ s: Suit) -> String {
          switch s {
          case .clubs: return "clubs"
          case .diamonds: return "diamonds"
          case .hearts: return "hearts"
          case .spades: return "spades"
          }
      }

      private var accessibilityText: String {
          if faceDown || card == nil { return "Face-down card" }
          return "\(rankName(card!.rank).capitalized) of \(suitName(card!.suit).capitalized)"
      }
  }

  (Verify actual Rank/Suit cases against BJSCore; adjust if case names differ. Do NOT modify BJSCore.)

Rewrite BJS/Views/Trainer/HandView.swift wholesale:

  import SwiftUI
  import BJSCore

  enum HandOverlap: CGFloat {
      case dealer = 0.30
      case player = 0.45
  }

  struct HandView: View {
      let cards: [Card]
      let faceDownIndices: Set<Int>
      let overlap: HandOverlap
      private let cardWidth: CGFloat = 88

      var body: some View {
          HStack(spacing: -cardWidth * overlap.rawValue) {
              ForEach(Array(cards.enumerated()), id: \.offset) { idx, card in
                  Group {
                      if faceDownIndices.contains(idx) {
                          CardView(faceDown: true)
                      } else {
                          CardView(card: card)
                      }
                  }
                  .frame(width: cardWidth)
              }
          }
          // DO NOT apply .clipped() — negative spacing requires overflow per Pitfall 4
      }
  }

Create BJSTests/CardAssetTests.swift (Swift Testing):
  import Testing
  import UIKit
  @testable import BJS
  import BJSCore

  @Test func allFaceCardsResolve() {
      for rank in Rank.allCases {
          for suit in Suit.allCases {
              let name = "\(rankNameFor(rank))_of_\(suitNameFor(suit))"
              #expect(UIImage(named: name) != nil, "Missing asset: \(name)")
          }
      }
  }

  @Test func cardBackResolves() {
      #expect(UIImage(named: "card_back") != nil)
  }

  (Helper functions or duplicate the rank/suit name mappers; keep test file self-contained.)

Create BJSTests/CardViewTests.swift:
  @Test func cardViewRendersAllCombinations() {
      for rank in Rank.allCases {
          for suit in Suit.allCases {
              let view = CardView(card: Card(rank: rank, suit: suit))
              let host = UIHostingController(rootView: view.frame(width: 88, height: 123))
              host.view.layoutIfNeeded()
              #expect(host.view.bounds.width > 0)
          }
      }
  }

  @Test func cardViewFaceDownRenders() {
      let host = UIHostingController(rootView: CardView(faceDown: true).frame(width: 88, height: 123))
      host.view.layoutIfNeeded()
      #expect(host.view.bounds.width > 0)
  }

Create BJSTests/HandViewTests.swift:
  @Test func handOverlapValues() {
      #expect(HandOverlap.dealer.rawValue == 0.30)
      #expect(HandOverlap.player.rawValue == 0.45)
  }

Add the three test files to project.yml sources and re-run XcodeGen. Ensure @testable import BJS exposes internal types (CardView, HandView, HandOverlap).
  </action>
  <verify>
    <automated>cd /Users/luke/BJS && xcodebuild test -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BJSTests/CardAssetTests -only-testing:BJSTests/CardViewTests -only-testing:BJSTests/HandViewTests 2>&1 | tail -10 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - BJS/Views/Trainer/CardView.swift contains `Image(assetName)` and NO `Image(systemName:` references
    - grep -c "systemName" BJS/Views/Trainer/CardView.swift returns 0
    - BJS/Views/Trainer/HandView.swift contains `enum HandOverlap` with cases dealer/player
    - grep "case dealer = 0.30" BJS/Views/Trainer/HandView.swift matches
    - grep "case player = 0.45" BJS/Views/Trainer/HandView.swift matches
    - grep -c "\.clipped()" BJS/Views/Trainer/HandView.swift returns 0 (Pitfall 4)
    - CardAssetTests, CardViewTests, HandViewTests all pass under xcodebuild test
    - Full BJSCoreTests + BJSTests suite still green
  </acceptance_criteria>
  <done>CardView renders real SVG art for any (rank, suit) and for face-down state. HandView overlaps cards via negative HStack spacing with differentiated dealer/player values. All three Wave-0 tests pass.</done>
</task>

</tasks>

<verification>
- `xcodebuild test -scheme BJS -destination 'platform=iOS Simulator,name=iPhone 16'` — full suite green
- Visual spot-check in Xcode Preview: render CardView for ace_of_spades, 10_of_hearts, king_of_clubs, and faceDown — all show non-blank real card art
- ATTRIBUTION.md reviewable; license excerpt present
</verification>

<success_criteria>
- 53 SVG card assets present with Preserve Vector Data
- CardView and HandView rebuilt against real art with differentiated overlap
- Wave-0 tests (CardAssetTests, CardViewTests, HandViewTests) passing
- License verification complete and documented
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-foundation-rebuild/07-02-SUMMARY.md`
</output>
